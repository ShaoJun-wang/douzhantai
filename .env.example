# 抖战台 · 真实版环境变量样例
# 用法：复制本文件为同目录 .env 并填入真实值；.env 已加入 .gitignore，不会被提交到赛事仓库。
# 所有凭据仅服务端（proxy.js）读取，绝不进入前端、绝不硬编码进源码。

# —— 阿里云百炼（真实 LLM 生成，可选；不填则前端走演示模式） ——
DASHSCOPE_API_KEY=sk-你的百炼API密钥

# —— 飞书（真实读取多维表格竞品库，可选；三项都填才激活 feishu_real 工具） ——
FEISHU_APP_ID=cli_你的飞书应用ID
FEISHU_APP_SECRET=你的飞书应用Secret
FEISHU_APP_TOKEN=bascn你的多维表格AppToken
# 可选：指定读取某个数据表（不填则用多维表格的第一个数据表）
# FEISHU_TABLE_ID=tbl_你的数据表ID

# —— 代理访问令牌（可选，强烈建议公网部署时设置） ——
# 设置后，调用 /api/feishu 必须带 Authorization: Bearer <此处值>，否则返回 401
# 前端设置面板「代理访问令牌」填同样的值即可（两端一致才通）
# PROXY_ACCESS_TOKEN=你的强随机令牌
